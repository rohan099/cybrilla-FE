import React from 'react';

const Header = () => {
    return (
        <div>
            <h1  className="header">Critical Illness Insurance Calculator</h1>
            <h5>Adjust the slider to estimate the coverage amount you may need</h5>
        </div>
    );
};

export default Header;